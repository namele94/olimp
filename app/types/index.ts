export interface Product {
  id: string;
  name: string;
  price: number;
  image: string;
  category: string;
  oldPrice: number;
  calories: string;
  weight: string;
}

export interface Event {
  id: string;
  title: string;
  time: string;
  subtitle: string;
}
